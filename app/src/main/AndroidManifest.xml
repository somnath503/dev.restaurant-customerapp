<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Allows the app to access precise location (e.g., GPS) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <!-- Declare that telephony hardware is not strictly required -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />


    <application
        android:name=".ApplicationClass"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Customer_app"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Customer_app.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.RegistrationActivity" />


        <!-- NEW MAIN APP ACTIVITY after login -->
        <activity android:name=".activities.MenuActivity"
            android:theme="@style/Theme.Customer_app.NoActionBar"/>
        <activity
            android:name=".activities.MenuItemDetailActivity"
            android:theme="@style/Theme.Customer_app.NoActionBar" />
        <activity
            android:name=".activities.RestaurantInfoActivity"
            android:exported="false"
            android:theme="@style/Theme.Customer_app.NoActionBar" />
        <activity android:name=".activities.OrderSuccessActivity"
            android:noHistory="true"
            android:launchMode="singleTop"/>

        <activity android:name=".activities.OrderDetailActivity" android:exported="false" />


        <activity android:name=".activities.CartActivity" />
        <activity android:name=".activities.CheckoutActivity" />
        <activity android:name=".activities.OrderHistoryActivity" />
        <activity android:name=".activities.PaymentActivity" />
        <activity android:name=".activities.ProfileActivity" />

    </application>
</manifest>